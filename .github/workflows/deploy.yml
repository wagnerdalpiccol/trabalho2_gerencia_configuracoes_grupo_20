name: Pipeline de Deploy Multi-Ambiente
run-name: Deploy em andamento para ${{ github.ref_name }} (Disparado por ${{ github.event_name }})

# --- GATILHOS (TRIGGERS) ---
on:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      target_environment:
        description: 'Selecione o ambiente para deploy manual (preprod ou dev)'
        required: true
        type: choice
        options:
          - preprod
          - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔍 Definir Variáveis de Ambiente e Branch Alvo
        id: set_env
        run: |
          echo "HOST_SUPABASE=db.furkyzfwvzptwtbkoacl.supabase.co" >> $GITHUB_ENV
          
          # 1. Triggers 'push' (master/main)
          if [[ "${{ github.event_name }}" == "push" && ("${{ github.ref_name }}" == "main" || "${{ github.ref_name }}" == "master") ]]; then
            echo "TARGET_ENV=prod" >> $GITHUB_ENV
            echo "DB_URL=${{ secrets.PROD_DB_URL }}" >> $GITHUB_ENV
            echo "DEPLOY_HOOK=${{ secrets.PROD_DEPLOY_HOOK }}" >> $GITHUB_ENV
            echo "TARGET_BRANCH=main" >> $GITHUB_ENV

          # 2. Trigger 'schedule' (preprod)
          elif [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "TARGET_ENV=preprod" >> $GITHUB_ENV
            echo "DB_URL=${{ secrets.PREPROD_DB_URL }}" >> $GITHUB_ENV
            echo "DEPLOY_HOOK=${{ secrets.PREPROD_DEPLOY_HOOK }}" >> $GITHUB_ENV
            echo "TARGET_BRANCH=preprod" >> $GITHUB_ENV

          # 3. Trigger 'workflow_dispatch' (manual - preprod ou dev)
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            ENV_CHOICE="${{ github.event.inputs.target_environment }}"
            echo "TARGET_ENV=$ENV_CHOICE" >> $GITHUB_ENV
            echo "TARGET_BRANCH=$ENV_CHOICE" >> $GITHUB_ENV
          
            if [[ "$ENV_CHOICE" == "preprod" ]]; then
              echo "DB_URL=${{ secrets.PREPROD_DB_URL }}" >> $GITHUB_ENV
              echo "DEPLOY_HOOK=${{ secrets.PREPROD_DEPLOY_HOOK }}" >> $GITHUB_ENV
            elif [[ "$ENV_CHOICE" == "dev" ]]; then
              echo "DB_URL=${{ secrets.DEV_DB_URL }}" >> $GITHUB_ENV
              echo "DEPLOY_HOOK=${{ secrets.DEV_DEPLOY_HOOK }}" >> $GITHUB_ENV
            fi
          
          # 4. Falha se não for um trigger válido para deploy
          else
            echo "Skipping deploy..."
            exit 0
          fi

      - name: ⚙️ Checkout do Código (Branch Alvo)
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}

      - name: 💾 Instalar Cliente PostgreSQL
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: 🚀 Deploy do Frontend (Página HTML)
        run: |
          echo "Disparando deploy do Frontend para o ambiente ${{ env.TARGET_ENV }}..."
          curl -X POST -d {} "${{ env.DEPLOY_HOOK }}"
          echo "Hook de Deploy enviado. Verifique o status no Vercel."

      - name: 🧱 INICIALIZAÇÃO Criar e Selecionar o Schema
        run: |
          SCHEMA_NAME="${{ env.TARGET_ENV }}"
          echo "Criando e selecionando o schema: $SCHEMA_NAME"
          
          # CRÍTICO: Usa psql -c para executar comandos diretamente, evitando problemas com o sed.
          # Cria o schema e o define como caminho de busca (search_path).
          psql "${{ env.DB_URL }}" -v ON_ERROR_STOP=1 -c "CREATE SCHEMA IF NOT EXISTS \"$SCHEMA_NAME\"; SET search_path TO \"$SCHEMA_NAME\", public;"
          
          echo "Schema $SCHEMA_NAME criado/selecionado com sucesso."


      - name: 🔄 Executar Migrações do Banco de Dados
        run: |
          echo "Iniciando migrações para o BD ${{ env.TARGET_ENV }}..."
          
          # O script V1 deve ser executado AGORA no schema selecionado no passo anterior.
          MIGRATION_FILE="grupo20/src/main/resources/db/migration/V1__cria_tabela_cliente.sql"

          if [ -f "$MIGRATION_FILE" ]; then
            echo "-> Executando migração: $MIGRATION_FILE"
            # O comando psql executa o arquivo DENTRO do schema selecionado no passo anterior.
            psql "${{ env.DB_URL }}" -f "$MIGRATION_FILE" -v ON_ERROR_STOP=1
          
            echo "Migração do banco de dados concluída com sucesso para o ambiente ${{ env.TARGET_ENV }}."
          else
            echo "❌ ERRO: Arquivo de migração $MIGRATION_FILE não encontrado."
            exit 1
          fi